name: Release Please

on:
  push:
    branches: [dev]
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: false

jobs:
  release-please:
    name: Release Please
    uses: NeoTamia/actions/.github/workflows/release-please.yml@c276da6d27dff237efafe0704d6b060450689ebf # main
    permissions:
      contents: write
      pull-requests: write
    secrets:
      RELEASE_PLEASE_PRIVATE_KEY: ${{ secrets.RELEASE_PLEASE_PRIVATE_KEY }}
    with:
      client-id: Iv23lizpKlycarJsIMmU

  gitflow-release:
    name: GitFlow Release
    needs: release-please
    if: ${{ needs.release-please.outputs.release_created == 'true' }}
    uses: NeoTamia/actions/.github/workflows/gitflow-release.yml@c276da6d27dff237efafe0704d6b060450689ebf # main
    permissions:
      contents: write
    secrets:
      RELEASE_PLEASE_PRIVATE_KEY: ${{ secrets.RELEASE_PLEASE_PRIVATE_KEY }}
    with:
      tag-name: ${{ needs.release-please.outputs.tag_name }}
      version: ${{ needs.release-please.outputs.version }}
      client-id: Iv23lizpKlycarJsIMmU
